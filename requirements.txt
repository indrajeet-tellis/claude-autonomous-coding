# Claude Agent SDK (includes bundled CLI)
claude-agent-sdk>=0.1.0

# For fallback/compatibility
anthropic>=0.39.0

# Utilities
pyyaml>=6.0
aiohttp>=3.9.0
aiofiles>=23.2.1
rich>=13.7.0
httpx>=0.27.0
python-dotenv>=1.0.0
anyio>=4.0.0
